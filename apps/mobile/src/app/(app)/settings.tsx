import React from 'react';
import { Text, View } from 'react-native';

export const Home = () => {
  return (
    <View>
      <Text>Settings</Text>
    </View>
  );
};

export default Home;
